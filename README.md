# gabclassic
A firefox extension to load the classic gab.com UI

# Hold tight

The old version had a hard coded user object, resulting in everyone being able to post as me (that's bad). This version now uses the v3 api provided by gab to set the initial-state in the app instead of it being embedded in the html.
